const config = {
  TradeAdmin: {
    // 启动端口
    port: 3000,
    // 数据库配置
    database: {
      DATABASE: 'TradeAdmin',
      USERNAME: 'root',
      PASSWORD: 'gmmdbdev',
      PORT: '3306',
      HOST: '10.240.17.237'
    }
  }
}

module.exports = config