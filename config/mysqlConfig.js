const config = {
  TradeAdmin: {
    // 启动端口
    port: 3000,
    // 数据库配置
    database: {
      DATABASE: 'TradeAdmin',
      USERNAME: 'root',
      PASSWORD: '',
      PORT: '3306',
      HOST: ''
    }
  }
}

module.exports = config